import 'package:flutter/material.dart';

class MenuButtonModel {
  String herotag;
  IconData? icon;
  Color? color;
  double bottomPosition;
  double leftPosition;
  MenuButtonModel(this.herotag, this.icon, this.color, this.bottomPosition, this.leftPosition);
}
