// ignore_for_file: file_names

import 'package:flutter/material.dart';

class MenuButtonModel {
  String herotag;
  int selectedPageNumber;
  IconData? icon;
  Color? color;
  double bottomPosition;
  double leftPosition;
  MenuButtonModel(this.herotag, this.selectedPageNumber, this.icon, this.color, this.bottomPosition, this.leftPosition);
}
